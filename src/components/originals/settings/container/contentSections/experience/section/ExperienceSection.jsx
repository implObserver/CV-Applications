import { Section } from '../../../../../prototypes/contentSection/section/Section';

export const ExperienceSection = ({ name, props }) => {
  return <Section name={name} props={props}></Section>;
};
