import { FormAddButton } from '../../../../../../../prototypes/contentSection/section/buttonWrapper/button/Button';

export const EducationAddButton = (props) => {
  const prototype = FormAddButton('Education', props);
  return Object.assign(prototype);
};
